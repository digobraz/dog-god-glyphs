Remove the "or see story first" text and the bottom ChevronDown arrow from `HeroSection.tsx`.

### Changes
- Delete the `motion.p` containing "or see story first"
- Delete the `motion.div` wrapping the `ChevronDown` icon
- Remove the now-unused `ChevronDown` import from `lucide-react`

### File
- `src/components/landing/HeroSection.tsx`