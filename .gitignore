# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Env files (publishable keys, but best practice to exclude)
.env
.env.local
.env.production
.env.development

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Claude Code local state (worktrees, agent logs)
.claude/

# local screenshot/tuning artifacts
.tune/
.about-*.png

# QA screenshot dumps (local, throwaway)
.qa-screens/
.gstack/

*.tsbuildinfo

# Jednorazové meracie a snímkovacie skripty (25. 9. 2026). Musia ležať TU, a nie
# v scratchpade, lebo `import { chromium } from 'playwright'` sa rozbaľuje proti
# node_modules tohto priečinka — ale do repa nepatria. Auto-save hook ich inak
# zoberie so sebou (stalo sa 24. 9.: päť `*.tmp.mjs` v commite).
*.tmp.mjs
